# 你的PHP项目调试指南 - 完整实战

## 🎉 成功案例：通过调试发现并修复了一个Bug！

我们刚刚通过调试发现并修复了一个真实的bug：
- **问题**：在 `SimUserService.php:92` 行，传递了字符串类型的点数给期望float类型的方法
- **解决方案**：添加了类型转换 `(float)$rechargeCode->points`
- **工具**：使用了项目自带的debug函数和日志查看工具

## 📋 调试步骤总结

### 1. 开启调试模式
编辑 `index.php`：
```php
const DEBUG = true;  // 原来是 false
```

### 2. 添加调试代码
在需要调试的地方调用：
```php
debug("调试信息: " . $variable);
```

### 3. 查看调试结果
使用我们创建的工具：
```bash
# 实时查看日志
./debug_tools.sh tail

# 查看最新日志
./debug_tools.sh show

# 搜索特定内容
./debug_tools.sh simclient  # 查看SimClient相关
./debug_tools.sh user       # 查看用户相关
./debug_tools.sh search 错误 # 搜索错误信息
```

## 🛠️ 实际使用示例

### 在你的SimClient功能中调试：

**控制器调试** - 已在 `app/Controller/User/SimClient.php` 中添加
```php
public function index(): string
{
    debug("=== SimClient页面开始加载 ===");
    // ... 你的代码
    debug("=== SimClient页面加载完成 ===");
}
```

**服务层调试** - 在任意服务类中添加
```php
// 例如在OrderService中
public function createOrder() {
    debug("开始创建订单...");
    debug("订单参数: " . json_encode($params));
    // ... 处理逻辑
    debug("订单创建完成");
}
```

**前端调试** - 在浏览器控制台查看
```javascript
// 打开浏览器开发者工具(F12) -> Console
console.log("SimClient前端调试");
console.log("订单状态:", orderStatus);
```

## 🔍 常见调试场景

### 1. 调试API接口
```php
// 在API控制器中
public function order() {
    debug("收到订单请求: " . json_encode($_POST));
    
    $result = $this->processOrder();
    
    debug("订单处理结果: " . json_encode($result));
    return $this->success($result);
}
```

### 2. 调试数据库查询
```php
// 在Model中
public function getUserOrders($userId) {
    debug("查询用户订单, userId: " . $userId);
    
    $orders = $this->where('user_id', $userId)->get();
    
    debug("查询结果: " . count($orders) . " 条记录");
    return $orders;
}
```

### 3. 调试错误
```php
try {
    // 可能出错的代码
    $result = riskyOperation();
} catch (Exception $e) {
    debug("操作失败: " . $e->getMessage());
    debug("错误堆栈: " . $e->getTraceAsString());
    throw $e;
}
```

## 📊 监控和日志管理

### 实时监控
```bash
# 监控所有调试信息
./debug_tools.sh tail

# 只看SimClient相关
./debug_tools.sh simclient

# 搜索错误
./debug_tools.sh search 错误
./debug_tools.sh search Exception
```

### 日志清理
```bash
# 清空调试日志（重要：生产环境定期清理）
./debug_tools.sh clear

# 备份重要日志
cp runtime.log runtime.log.backup
```

## 🚀 快速开始清单

- [ ] 编辑 `index.php` 开启 `DEBUG = true`
- [ ] 在需要调试的代码中添加 `debug("信息")`
- [ ] 访问页面或发送请求
- [ ] 运行 `./debug_tools.sh tail` 查看实时日志
- [ ] 使用 `./debug_tools.sh show` 查看最新日志
- [ ] 修复发现的问题
- [ ] 清理调试代码或关闭DEBUG模式

## 💡 调试技巧

1. **有意义的调试信息**：不要只写"测试"，要写"用户ID=123创建订单"
2. **分层调试**：在控制器、服务层、模型层都添加适当的调试信息
3. **数据类型检查**：用 `var_dump()` 检查变量类型
4. **错误捕获**：用 try-catch 包装可能出错的代码
5. **前端调试**：结合浏览器开发者工具调试JavaScript

## 🔧 进阶调试工具

如果需要更强大的调试功能，可以考虑：
- **Xdebug**：断点调试和代码覆盖
- **PHPUnit**：单元测试
- **Laravel Telescope**：请求监控（如果使用Laravel）

## ⚠️ 生产环境注意

1. **关闭调试**：生产环境将 `DEBUG = false`
2. **清理日志**：定期清理 `runtime.log` 文件
3. **移除调试代码**：删除或注释掉调试用的 `debug()` 调用
4. **错误日志**：配置PHP错误日志到独立文件

---

**现在你可以开始调试你的SimClient功能了！** 🎯

使用 `./debug_tools.sh help` 查看所有可用命令。